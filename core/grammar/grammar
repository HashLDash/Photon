comment = hashtag

string = singleQuote
    | doubleQuote

var = var underline var
    | underline var
    | var underline
    | underline

floatNumber = num dot num

printFunction = print lparen (expr num) rparen
