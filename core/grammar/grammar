comment = hashtag

string = singleQuote
    | doubleQuote

typeDeclaration = var var
                | type var

var = var underline var
    | underline var
    | var underline
    | underline

floatNumber = num dot num
            | num dot

expr = num
     | floatNumber
     | var

printFunc = print lparen (expr) rparen
