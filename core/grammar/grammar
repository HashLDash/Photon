comment = hashtag

string = singleQuote
    | doubleQuote

var = var underline var
    | underline var
    | var underline
    | underline

floatNumber = num dot num
            | num dot

expr = num
     | floatNumber
     | var

printFunction = print lparen (expr) rparen
